// start xampp and phpmyadmin

sudo /opt/lampp/lampp start

// to stop
sudo /opt/lampp/lampp stop


// for software update
========================
sudo rm -rvf /var/lib/apt/lists/* 
// and then:
sudo apt-get update
// You may need to reboot computer for it to effect completely by typing:
sudo shutdown -r now

// for bluetooth
sudo service bluetooth
{start|stop|restart|force-reload|status}

// for bluetooth proplems check
https://askubuntu.com/questions/787023/bluetooth-not-working-on-ubuntu-16-04-lts


// to change the file permision 

sudo chmod -R 777 /yourpath


If you want to create ZIP files using Ubuntu (or almost any other Linux), use zip. You can install it to Ubuntu by running

sudo apt-get install zip
Then you can create zip file by running

zip -r compressed_filename.zip foldername
